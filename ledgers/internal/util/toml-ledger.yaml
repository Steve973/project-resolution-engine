docKind: derived-ids
schemaVersion: 1.0.0
unit:
  name: toml
  language: python
  unitId: C000
assigned:
  entries:
  - id: C000F001
    kind: callable
    name: load_toml_file
    address: toml::load_toml_file@L11
  - id: C000F002
    kind: callable
    name: load_toml_text
    address: toml::load_toml_text@L35
  - id: C000F003
    kind: callable
    name: dump_toml_to_str
    address: toml::dump_toml_to_str@L52
  - id: C000F004
    kind: callable
    name: dump_toml_to_file
    address: toml::dump_toml_to_file@L71
  branches:
  - id: C000F001E0001
    address: toml::function001@L31
    summary: open(path, 'rb') succeeds → context manager enters successfully
  - id: C000F001E0002
    address: toml::function001@L31
    summary: open(path, 'rb') raises FileNotFoundError → exception propagates
  - id: C000F001E0003
    address: toml::function001@L31
    summary: open(path, 'rb') raises PermissionError → exception propagates
  - id: C000F001E0004
    address: toml::function001@L31
    summary: open(path, 'rb') raises other exception → exception propagates
  - id: C000F001E0005
    address: toml::function001@L32
    summary: tomli.load(f) succeeds → returns parsed dict
  - id: C000F001E0006
    address: toml::function001@L32
    summary: tomli.load(f) raises tomli.TOMLDecodeError → exception propagates
  - id: C000F001E0007
    address: toml::function001@L32
    summary: tomli.load(f) raises other exception → exception propagates
  - id: C000F002E0001
    address: toml::function002@L49
    summary: tomli.loads(text) succeeds → returns parsed dict
  - id: C000F002E0002
    address: toml::function002@L49
    summary: tomli.loads(text) raises tomli.TOMLDecodeError → exception propagates
  - id: C000F002E0003
    address: toml::function002@L49
    summary: tomli.loads(text) raises other exception → exception propagates
  - id: C000F003E0001
    address: toml::function003@L68
    summary: tomli_w.dumps(data, indent=indent) succeeds → returns TOML string
  - id: C000F003E0002
    address: toml::function003@L68
    summary: tomli_w.dumps(data, indent=indent) raises exception → exception propagates
  - id: C000F004E0001
    address: toml::function004@L86
    summary: Path(path) construction succeeds, dump_toml_to_str(data) succeeds, write_text(..
  - id: C000F004E0002
    address: toml::function004@L86
    summary: Path(path) construction raises exception → exception propagates
  - id: C000F004E0003
    address: toml::function004@L86
    summary: dump_toml_to_str(data) raises exception → exception propagates
  - id: C000F004E0004
    address: toml::function004@L86
    summary: write_text(...) raises exception → exception propagates
---
docKind: ledger
schemaVersion: 1.0.0
unit:
  id: C000
  kind: unit
  name: toml
  children:
  - id: C000F001
    kind: callable
    name: load_toml_file
    signature: 'load_toml_file(path: str | Path) -> dict[str, Any]'
    callable:
      params:
      - name: path
        type:
          name: str | Path
      returnType:
        name: dict[str, Any]
      branches:
      - id: C000F001E0001
        condition: with open(path, ''rb'')
        outcome: context manager enters successfully
      - id: C000F001E0002
        condition: open(path, ''rb'') raises FileNotFoundError
        outcome: exception propagates
      - id: C000F001E0003
        condition: open(path, ''rb'') raises PermissionError
        outcome: exception propagates
      - id: C000F001E0004
        condition: open(path, ''rb'') raises other exception
        outcome: exception propagates
      - id: C000F001E0005
        condition: tomli.load(f) succeeds
        outcome: returns parsed dict
      - id: C000F001E0006
        condition: tomli.load(f) raises tomli.TOMLDecodeError
        outcome: exception propagates
      - id: C000F001E0007
        condition: tomli.load(f) raises other exception
        outcome: exception propagates
      integration:
        interunit:
        - id: IC000F001E0005
          target: tomli.load
          kind: call
          signature: tomli.load(f)
          executionPaths:
          - - C000F001E0001
            - C000F001E0005
          notes: Parses TOML from file object
        boundaries:
        - id: IC000F001E0001
          target: open
          kind: io
          signature: open(path, ''rb'')
          boundary:
            kind: filesystem
            operation: read
          executionPaths:
          - - C000F001E0001
          notes: Opens file for binary reading
  - id: C000F002
    kind: callable
    name: load_toml_text
    signature: 'load_toml_text(text: str) -> dict[str, Any]'
    callable:
      params:
      - name: text
        type:
          name: str
      returnType:
        name: dict[str, Any]
      branches:
      - id: C000F002E0001
        condition: tomli.load(f) succeeds
        outcome: returns parsed dict
      - id: C000F002E0002
        condition: tomli.load(f) raises tomli.TOMLDecodeError
        outcome: exception propagates
      - id: C000F002E0003
        condition: tomli.load(f) raises other exception
        outcome: exception propagates
      integration:
        interunit:
        - id: IC000F002E0001
          target: tomli.loads
          kind: call
          signature: tomli.loads(text)
          executionPaths:
          - - C000F002E0001
          notes: Parses TOML from string
  - id: C000F003
    kind: callable
    name: dump_toml_to_str
    signature: 'dump_toml_to_str(data: Mapping[str, Any], indent: int = 2) -> str'
    callable:
      params:
      - name: data
        type:
          name: Mapping[str, Any]
      - name: indent
        type:
          name: int
        default: 2
      returnType:
        name: str
      branches:
      - id: C000F003E0001
        condition: tomli_w.dumps(data, indent=indent) succeeds
        outcome: returns TOML string
      - id: C000F003E0002
        condition: raises exception
        outcome: exception propagates
      integration:
        interunit:
        - id: IC000F003E0001
          target: tomli_w.dumps
          kind: call
          signature: tomli_w.dumps(data, indent=indent)
          executionPaths:
          - - C000F003E0001
          notes: Serializes data to TOML string
  - id: C000F004
    kind: callable
    name: dump_toml_to_file
    signature: 'dump_toml_to_file(data: Mapping[str, Any], path: str | Path) -> None'
    callable:
      params:
      - name: data
        type:
          name: Mapping[str, Any]
      - name: path
        type:
          name: str | Path
      returnType:
        name: None
      branches:
      - id: C000F004E0001
        condition: Path(path) construction succeeds
        outcome: writes file and returns None
      - id: C000F004E0002
        condition: raises exception
        outcome: exception propagates
      - id: C000F004E0003
        condition: raises exception
        outcome: exception propagates
      - id: C000F004E0004
        condition: raises exception
        outcome: exception propagates
      integration:
        interunit:
        - id: IC000F004E0001
          target: Path
          kind: construct
          signature: Path(path)
          executionPaths:
          - - C000F004E0001
          notes: Constructs Path object from path parameter
        boundaries:
        - id: IC000F004E0001
          target: Path.write_text
          kind: io
          signature: Path(path).write_text(dump_toml_to_str(data), encoding=''utf-8'')
          boundary:
            kind: filesystem
            operation: write
          executionPaths:
          - - C000F004E0001
          notes: Writes TOML string to file with UTF-8 encoding
---
docKind: ledger-generation-review
schemaVersion: 1.0.0
unit:
  name: toml
  language: python
  callablesAnalyzed: 4
  totalExeItems: 16
  interunitIntegrations: 4
  boundaryIntegrations: 2
findings: []