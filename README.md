# project-resolution-engine

A small, provider-driven dependency resolution engine (wheel-focused) intended to be reusable
across tools. This repo is intentionally decoupled from any specific build/bundling system.

## Layout
- `src/project_resolution_engine/`: engine code
- `tests/`: tests
